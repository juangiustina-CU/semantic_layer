---
name: Sessions
description: "Migrated from Eppo fact: Sessions"
tags:
  - Eppo
  - Migrated
  - Semantic Layer
sql: |
  SELECT
      *,
      DATEDIFF(minute, session_start_ts, session_end_ts) AS session_duration
  FROM
      CU_DBT_PROD.ENTITIES_FACT.FACT_MARKETPLACE_EVENTS
timestampColumn: VISITED_TS
timestampAsDay: false
idTypeMapping:
  - statsigUnitID: userID
    column: CUSTOMER_ID