---
name: Churns
description: "Migrated from Eppo fact: Churns"
tags: []
sql: |
  SELECT
      *
  FROM
      CU_DBT_PROD.SILVER_FACT.FACT_EVENTS_SNAPSHOTS
  WHERE
      EVENT IN ('unsubscribe')
timestampColumn: EVENT_DATE
timestampAsDay: true
idTypeMapping:
  - statsigUnitID: userID
    column: CUSTOMER_ID