---
name: Pauses
description: "Migrated from Eppo fact: Pauses"
tags: []
sql: |
  SELECT
      *
  FROM
      CU_DBT_PROD.SILVER_FACT.FACT_EVENTS_SNAPSHOTS
  WHERE
      EVENT IN ('pause')
timestampColumn: EVENT_DATE
timestampAsDay: true
idTypeMapping:
  - statsigUnitID: userID
    column: CUSTOMER_ID